using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// Essentially a dummy component to allow ComponentFinder locate
// the transform that is local only (The parent for everything that isnt a child of the NetworkTransform)
public class LocalTransform : MonoBehaviour
{
	
}
